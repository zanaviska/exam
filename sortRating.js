'use strict';

const sortRating = (array) => {
   compareRating(personA, personB) {
    return personB.rating - personA.rating;
  }
  array.sort((personA, personB) =>
      personB.rating - personA.rating));
  return array;
};

module.exports = sortRating;
